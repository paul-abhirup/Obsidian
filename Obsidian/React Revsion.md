## - How JSX works under the hood (transpiled to `React.createElement`).

JSX is a syntax that alllows js devs to write html like code on  React componnet
which is the transpiled to `React.createElement`